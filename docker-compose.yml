version: '3.8'

services:
  task-api:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./database:/var/www/html/database
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html
    restart: unless-stopped 